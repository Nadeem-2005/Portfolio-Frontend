export const featuredRepositories = [
    //repository names
    "CliniCall",
    "Portfolio-Frontend",
    "Portfolio-Backend",
    "CLI-Chatbot",
    "Watermarking-based-on-DWT-SVD",
    "Haar-based-image-Steganography",
    "The-Climate"
];

export const githubUsername = 'Nadeem-2005';

export const maxRepositories = 6;

// Repository display preferences
export const repositoryConfig = {
    // Whether to fallback to recent repositories if featured ones aren't found
    fallbackToRecent: true,

    // Minimum description length to consider a repository
    minDescriptionLength: 10,

    // Whether to exclude forked repositories
    excludeForks: true,

    // Whether to only show public repositories
    publicOnly: true
};